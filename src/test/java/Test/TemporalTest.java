package Test;

import modelo.Temporal;
import modelo.IncentivoAntiguedad;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class TemporalTest {
/*
    @Test
    public void testSalarioQuincenal() {
        Temporal emp = new Temporal("208760456", "Laura Jimenez",
                new IncentivoAntiguedad(), 3,
                2000.0, 10);

        double expected = 2000.0 * 10;

        assertEquals(expected, emp.salarioQuicena(), 0.001,
                "El salario quincenal no coincide con la fórmula de la clase Temporal");
    }

    @Test
    public void testBono() {
        Temporal emp = new Temporal("208760456", "Laura Jimenez",
                new IncentivoAntiguedad(), 3,
                2000.0, 10);

        double expectedBono = 100 * emp.getAniosServicio();

        assertEquals(expectedBono, emp.bono(), 0.01,
                "El bono por antigüedad no coincide");
    }

    @Test
    public void testNombreYCedula() {
        Temporal emp = new Temporal("208760456", "Laura Jimenez",
                new IncentivoAntiguedad(), 3,
                2000.0, 10);

        assertEquals("Laura Jimenez", emp.getNombre(), "El nombre no coincide");
        assertEquals("208760456", emp.getCedula(), "La cédula no coincide");
    }

    @Test
    public void testToCSV() {
        Temporal emp = new Temporal("208760456", "Laura Jimenez",
                new IncentivoAntiguedad(), 3,
                1500.0, 5);

        String expected = "TEMPORAL;208760456;Laura Jimenez;1500.0;5";
        assertEquals(expected, emp.toCSV(),
                "El formato CSV no coincide con el esperado");
    }
*/
}
